---
// src/pages/Index.astro
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import ImageCarousel from '../components/ImageCarousel.jsx';
import { works } from '../data/paintings.jsx';
import ReservationForm from '../components/reservations/ReservationForm.jsx';
import Layout from '../layouts/Layout.astro';
import { menuItems } from '../data/menuData.jsx'; 
import { reviews } from '../data/reviews.jsx'; 
import StarRating from '../components/StarRating.astro';

const checkIcon = `
  <svg class="w-6 h-6 text-yellow-400" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
  </svg>
`;
---
<Layout 
  title="Restaurante Van Gogh – Comida Peruana en Huamachuco" 
  description="Vive una experiencia única en Van Gogh, restaurante en Huamachuco con auténtica comida peruana y un ambiente artístico inspirado en el genio del postimpresionismo."
  keywords="Restaurante Van Gogh Huamachuco, comida peruana tradicional, restaurante temático en Perú, experiencia gastronómica artística, mejor restaurante en Huamachuco, cocina peruana gourmet"
>
	  	<Header />

  	<main>
		  		<section class="relative min-h-screen flex items-center justify-center text-center bg-cover bg-center" style="background-image: url('/Van_Gogh.jpg');">
  			<div class="absolute inset-0 bg-black bg-opacity-50"></div>
  			<div class="relative z-10 px-4">
  				<h1 class="text-6xl md:text-8xl font-bold text-yellow-300 drop-shadow-lg">"Donde cada bocado es una obra de arte"</h1>
  				<p class="text-xl mt-4 max-w-2xl mx-auto text-white">
  					Una experiencia culinaria inspirada en la pasión y el color de Vincent van Gogh.
  				</p>
  				<div class="mt-8 flex flex-col sm:flex-row justify-center gap-4">
  					<a href="#reservar" class="inline-block bg-yellow-400 text-blue-900 font-bold py-3 px-8 rounded-full text-lg hover:bg-yellow-300 transition-transform duration-300 hover:scale-105">
  						Reservar una Mesa
  					</a>
  					<a href="#cartas" class="inline-block bg-transparent border-2 border-yellow-400 text-yellow-400 font-bold py-3 px-8 rounded-full text-lg hover:bg-yellow-400 hover:text-blue-900 transition-all duration-300">
  						Ver la Carta
  					</a>
  				</div>
  			</div>
  		</section>

            <section id="bienvenida" class="py-20 bg-black bg-opacity-20 text-white overflow-hidden js-scroll-animate opacity-0 translate-y-10 transition-all duration-700 ease-in-out">
            <div class="container mx-auto px-4">
              
              <div class="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
                
                <div class="text-left">
                  <h2 class="text-4xl lg:text-5xl font-semibold mb-6 text-yellow-300 font-serif">
                    ¡Bienvenido a Van Gogh Fusiones!
                  </h2>
                  <p class="text-lg leading-relaxed mb-8 text-gray-300">
                    En este espacio, la pasión por el arte y la gastronomía se unen para ofrecerte una experiencia única. Inspirados en la vida y obra de Vincent Van Gogh, creamos un ambiente acogedor y creativo.
                  </p>

                  <h3 class="text-3xl font-semibold text-yellow-300 mb-6 font-serif">Nuestras Especialidades</h3>
                  
                  <ul class="space-y-4 text-lg text-gray-200">
                    <li class="flex items-center gap-3">
                      <Fragment set:html={checkIcon} />
                      <span>Fusión de carnes a la parrilla y carbón</span>
                    </li>
                    <li class="flex items-center gap-3">
                      <Fragment set:html={checkIcon} />
                      <span>Comida rápida de autor</span>
                    </li>
                    <li class="flex items-center gap-3">
                      <Fragment set:html={checkIcon} />
                      <span>Café de especialidad</span>
                    </li>
                    <li class="flex items-center gap-3">
                      <Fragment set:html={checkIcon} />
                      <span>Coctelería creativa</span>
                    </li>
                  </ul>

                  <a href="#cartas" class="mt-10 inline-block bg-yellow-400 text-blue-900 font-bold py-3 px-8 rounded-full text-lg hover:bg-yellow-300 transition-transform duration-300 hover:scale-105">
                    Explora Nuestras Cartas
                  </a>
                </div>
                
                <div class="hidden lg:block">
                  
                  <img 
                    src="/Van_Gogh_Condores.jpeg" 
                    alt="Ambiente acogedor del restaurante Van Gogh Fusiones"
                    class="rounded-lg shadow-2xl w-full h-auto object-cover"
                    style="max-height: 600px;"
                  >
                </div>

              </div>
            </div>
          </section>

		  		<section id="cartas" class="py-20 container mx-auto px-4 items-center js-scroll-animate opacity-0 translate-y-10 transition-all duration-700 ease-in-out">
  			<h2 class="text-5xl font-semibold mb-12 text-center text-yellow-300">Nuestra Galería de Sabores</h2>
  			<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-12 ">
  				{menuItems.map(item => (
  					<a href={`/cartas/${item.slug}`} class="block bg-[#122a51] rounded-lg shadow-xl overflow-hidden group transform hover:-translate-y-2 transition-transform duration-300">
  						<img src={item.image} alt={item.name} class="w-full h-64 object-cover" />
  						<div class="p-6">
  							<h3 class="text-2xl font-bold text-yellow-300 mb-2">{item.name}</h3>
  							<p class="text-gray-300 mb-4">{item.description}</p>
  							<div class="text-right text-2xl font-bold text-yellow-400">{item.price}</div>
  						</div>
  					</a>
  				))}
  			</div>
  		</section>

		  		<section id="filosofia" class="py-20 bg-black bg-opacity-20 js-scroll-animate opacity-0 translate-y-10 transition-all duration-700 ease-in-out">
			<div class="container mx-auto px-4 flex flex-col lg:flex-row items-center gap-12">
				<div class="lg:w-1/2 text-white">
					<h2 class="text-4xl font-semibold mb-6 text-yellow-300">Inspirados por un Genio</h2>
					<blockquote class="border-l-4 border-yellow-400 pl-4 italic text-gray-300 mb-6">
						<p>"La vida es una lucha, pero el arte es la luz que nos guía"</p>
						<footer class="text-right not-italic">- Vincent Van Gogh</footer>
					</blockquote>
					<p class="leading-relaxed mb-4">
						Vincent van Gogh no solo pintaba, volcaba su alma en cada pincelada. Nosotros buscamos esa misma pasión en nuestra cocina.
					</p>
					<p class="leading-relaxed mb-4">
						Nuestro restaurante está decorado con réplicas de sus obras más famosas, creando un ambiente que te sumerge en su mundo.
					</p>
					<h3 class="text-2xl font-semibold mt-6 mb-2 text-yellow-300">Arte y Gastronomía</h3>
					<p class="leading-relaxed">
						La unión perfecta de dos pasiones que nos inspiran a crear experiencias únicas. Te invitamos a experimentar una propuesta diferente en Huamachuco.
					</p>
				</div>
				<div class="lg:w-1/2 w-full">
					<ImageCarousel works={works} client:load /> 
				</div>
			</div>
  		</section>

		  		<section id="reseñas" class="py-20 container mx-auto px-4 js-scroll-animate opacity-0 translate-y-10 transition-all duration-700 ease-in-out">
  			<h2 class="text-5xl font-semibold mb-12 text-center text-yellow-300">Lo que dicen nuestros Clientes</h2>
  			<div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
  				{reviews.map(review => (
  					<div class="bg-[#122a51] p-6 rounded-lg shadow-xl flex flex-col">
  						<div class="mb-4">
  							<StarRating rating={review.rating} />
  						</div>
  						<blockquote class="text-gray-300 italic mb-4 flex-grow">
  							"{review.text}"
  						</blockquote>
  						<footer class="text-yellow-400 font-bold text-right">
  							- {review.author}
  						</footer>
  					</div>
  				))}
  			</div>
  		</section>

		  		<section id="reservar" class="py-20 container mx-auto px-4 js-scroll-animate opacity-0 translate-y-10 transition-all duration-700 ease-in-out">
  			<h2 id="contacto" class="text-5xl font-semibold mb-12 text-center text-yellow-300">Visítanos</h2>
  			<div class="grid grid-cols-1 lg:grid-cols-2 gap-12">
  				<ReservationForm client:load />
  				<div class="text-white">
					<div class="bg-[#122a51] p-8 rounded-lg shadow-xl mb-8">
						<h3 class="text-3xl font-bold text-yellow-300 mb-6">Información de Contacto</h3>
						<div class="space-y-4 text-lg">
							<p><strong>Dirección:</strong> Plazuela Tauricuxi, Huamachuco</p>
							<p><strong>Teléfono:</strong> 970154015</p>
							<p><strong>Horarios:</strong> 6:00 am a 12:00 pm (media noche)</p>
						</div>
						<p class="mt-6 text-xl text-yellow-400">¡Esperamos verte pronto!</p>
					</div>
  					<div class="w-full h-96 rounded-lg overflow-hidden shadow-md">
  						<iframe
  							width="100%"
  							height="100%"
  							style="border:0;"
  							loading="lazy"
  							allowfullscreen
  							referrerpolicy="no-referrer-when-downgrade"
							  							src="https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d247.04754168962518!2d-78.04999073130865!3d-7.815139722547777!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x91ade598efd7c1d9%3A0x6a22b943df7ad8d!2sParque%20Tauricuxi!5e0!3m2!1ses-419!2spe!4v1759030298338!5m2!1ses-419!2spe" 
  						>
  						</iframe>
  					</div>
  				</div>
  			</div>
  		</section>
  	</main>
  
  	<Footer />
<script>

		const observerOptions = {
			root: null, 
			rootMargin: '0px',
			threshold: 0.1 
		};

		const observerCallback = (entries, observer) => {
			entries.forEach(entry => {
				if (entry.isIntersecting) {
					entry.target.classList.remove('opacity-0', 'translate-y-10');
					
					observer.unobserve(entry.target);
				}
			});
		};

		const observer = new IntersectionObserver(observerCallback, observerOptions);

		const elementsToAnimate = document.querySelectorAll('.js-scroll-animate');
		
		elementsToAnimate.forEach(element => {
			observer.observe(element);
		});
	</script>
</Layout>